/* ============================================================
   골라담기 옵션 표시 데이터 (설정 전용)
   - 여기 값만 바꾸면 카드 표시가 바뀝니다. (구매 로직 X)
   - price/discount/perUnit 는 화면 표시용 문자열이며,
     실제 결제 금액은 카페24 기본 옵션/장바구니 로직을 그대로 사용합니다.
   - price/perUnit 은 테스트 상품 실제 옵션가 기준(10/20/30/50천원). 상품이 바뀌면 여기 값만 교체.
   ============================================================ */
window.PICK_OPTION_CONFIG = {
  // 옵션값 끝의 _1, _2 를 개입수별 추가 가능 횟수로 해석하는 패턴
  optionSuffixPattern: /_(\d+)$/,

  groups: {
    '10개입': {
      price: '10,000원',        // 표시용 실제가(카페24 옵션가와 일치)
      discount: '',             // 할인율 문구(기준가 없어 미표시)
      perUnit: '1개 : 1,000원', // 개당 가격(10,000 / 10)
      badge: '',                // 뱃지 문구(없으면 미표시)
      badgeType: 'accent',      // 'accent'(주황) | 'max'(빨강)
      order: 1,
    },
    '30개입': {
      price: '20,000원',
      discount: '',
      perUnit: '1개 : 667원',   // 20,000 / 30
      badge: '가장 많이 사요',
      badgeType: 'accent',
      order: 2,
    },
    '50개입': {
      price: '30,000원',
      discount: '',
      perUnit: '1개 : 600원',   // 30,000 / 50
      badge: '',
      badgeType: 'accent',
      order: 3,
    },
    '100개입': {
      price: '50,000원',
      discount: '',
      perUnit: '1개 : 500원',   // 50,000 / 100
      badge: '최대할인',
      badgeType: 'max',
      order: 4,
    },
  },
};
