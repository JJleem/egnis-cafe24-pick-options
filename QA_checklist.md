# 골라담기 옵션 UI — QA 확인 절차

## 0. 사전 준비

1. 편집한 디자인을 **저장** 후 **"대표 디자인"으로 설정**한다.
   (저장만 하면 실제 스토어프론트에 반영되지 않음)
2. **시크릿 창(로그아웃 상태)** 으로 스토어프론트 URL 접속
   `https://내몰도메인.cafe24.com/product/detail.html?product_no=...`
3. PC 브라우저 + 개발자도구 **모바일 뷰**(375px 등)를 함께 열어둔다.
4. 콘솔에 `[PickOptions] 초기화 완료` 로그가 뜨는지 확인
   (안 뜨면 옵션 버튼 셀렉터/마운트 영역 문제 → `js/pick-options.js`의 `findNativeOptionButtons` 점검)

---

## 1. 커스텀 옵션 UI 노출 (#1)

- [ ] 상품 상세 옵션 영역에 골라담기 카드 4종(10/30/50/100개입)이 보인다
- [ ] 각 카드에 뱃지·설명·추천 문구·`0 / N` 카운트·가격이 표시된다
- [ ] 카페24 기본 텍스트버튼 옵션은 숨겨져 있어도 DOM에는 남아있다
      (콘솔: `document.querySelector('[module="product_option"]')` 존재 확인)

---

## 2. 선택 상태 · 구매 흐름 동기화 (#2)  📹 녹화 대상

- [ ] 카드 클릭 → 카페24 **선택상품 목록(#totalProducts)** 에 해당 옵션 품목이 추가된다
- [ ] 추가되면 카드 테두리/배경이 액센트색으로 바뀌고 `aria-pressed="true"` 로 변한다
- [ ] 카운트가 `0 / N` → `1 / N` 으로 증가한다
- [ ] **선택상품 목록에서 품목을 직접 삭제(×)** 하면 카드 카운트/활성화 상태가 자동으로 되돌아온다
      (MutationObserver 동기화 확인)

---

## 3. 중복 방지 · 반복 클릭 (#3)  📹 녹화 대상

- [ ] 같은 카드를 **빠르게 여러 번 연타** 해도 남은 횟수만큼만 추가된다 (pending 잠금)
- [ ] 이미 담긴 옵션값이 중복으로 두 번 생성되지 않는다
- [ ] 담기 → 삭제 → 다시 담기 반복 시 오류 없이 정상 동작한다

---

## 4. suffix(_1, _2) 기준 횟수 제한 (#4)  📹 녹화 대상

옵션값 구성: `10개입_1/_2`, `30개입_1/_2`, `50개입_1/_2`, `100개입_1/_2` (각 최대 2회)

- [ ] 30개입 카드 2번 클릭 → `2 / 2`, 선택상품에 `30개입_1`, `30개입_2` 두 품목 생성
- [ ] 3번째 클릭 → 추가되지 않고 안내 메시지
      `"30개입 구성은 최대 2회까지 추가할 수 있습니다."` 노출
- [ ] `_1`만 있는 구성(예시 세팅 시)이라면 최대 1회까지만 추가되는지 확인
- [ ] 카드가 `is-complete` 상태로 표시된다

---

## 5. 금액 계산 · 품목 생성 (#5)

- [ ] 개입 수별 금액 차이가 선택상품 목록 금액에 반영된다
- [ ] 옵션값 8개가 모두 품목으로 등록되어 있어 각각 담을 수 있다
- [ ] 카드에 표시된 `displayPrice`(설정 JS 값)와 실제 카페24 금액의 의도가 일치한다

---

## 6. 수량 · 총금액 · 장바구니 · 바로구매 (#6)

- [ ] 선택상품 목록에서 **수량 변경** 시 총금액이 재계산된다
- [ ] **장바구니 담기** → 장바구니에 담긴 옵션/수량/금액이 정확하다
- [ ] **바로 구매** → 주문서로 정상 이동, 옵션/금액이 유지된다
- [ ] (커스텀 UI가 구매 로직을 대체하지 않고 카페24 기본 로직을 그대로 사용하는지 확인)

---

## 7. 활성화 상태 표시 (#7)  📹 녹화 대상

- [ ] 선택된 카드: 액센트 테두리 + 틴트 배경 + inset 링
- [ ] 카운트 색상이 액센트색으로 강조된다
- [ ] 모두 담은 카드: `is-complete` 표시
- [ ] 키보드 `Tab` 포커스 시 `:focus-visible` 링이 보인다 (접근성)

---

## 8. 설정 JS 분리 (#8)

- [ ] 뱃지/설명/추천/가격/노출 순서 등 표시 데이터가 `js/pick-options.config.js`에만 존재
- [ ] `config.js`의 값만 바꿔도 카드 표시가 바뀌는지 1개 값 수정 테스트
- [ ] 구매 로직/DOM 조작은 `pick-options.js`, 표시값은 `config.js`로 역할 분리 확인

---

## 9. PC / 모바일 반응형 (#9)

- [ ] PC: 카드 2열 그리드, 간격/정렬 깨짐 없음
- [ ] 모바일 뷰(≤420px): 1열 전환, 텍스트·버튼 줄바꿈 깨짐 없음
- [ ] 카드 터치 영역과 하단 **구매 버튼 영역이 겹치지 않음**
- [ ] 긴 문구(추천/설명)에서도 레이아웃이 무너지지 않음

---

## 녹화(GIF/영상) 필수 항목

리뷰 편의를 위해 아래 4개는 짧은 화면 녹화를 첨부:

- **#2** 카드 클릭 ↔ 선택상품 목록 동기화 (담기/삭제 양방향)
- **#3** 연타 시 중복 방지
- **#4** suffix 횟수 제한 + 초과 시 안내 메시지
- **#7** 선택 활성화 상태 표시

---

## 콘솔에서 빠르게 확인하는 스니펫

```js
// 커스텀 UI가 인식한 옵션 그룹 확인
document.querySelector('#pickOptions'); // 마운트 확인
// 초기화 로그: [PickOptions] 초기화 완료 { 10개입: [...], 30개입: [...], ... }

// 현재 선택상품 목록 텍스트
document.querySelector('#totalProducts')?.innerText;
```

## 미구현 / 제한 사항 (제출 시 기재)

- (여기에 실제 확인 후 발견된 제한/미구현 항목 기록)
- 모바일: 모달 대신 옵션 영역 바로 노출 방식 채택 (사유 기재)
